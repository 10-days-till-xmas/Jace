name: Test Jace
on:
  workflow_run:
    workflows: ["Build"]
    types: [completed]
    
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest ]
        target-framework: [ net5.0, net9.0 ]
        
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Fail if disabled
        if: ${{ github.event.workflow_run.conclusion == 'failure' }}
        run: |
          echo "❌ Build job failed"
          exit 1

      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Install .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            9.0.x
            5.0.x
      
      - name: Restore solution
        run: dotnet restore Jace.sln

      - name: Build and run tests
        shell: bash
        run: |
          dotnet test Jace.sln \
            -f ${{ matrix.target-framework }} \
            --results-directory ./test-results/${{ matrix.target-framework }}-${{ matrix.os }}

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.target-framework }}-${{ matrix.os }}
          path: ./test-results/${{ matrix.target-framework }}-${{ matrix.os }}
